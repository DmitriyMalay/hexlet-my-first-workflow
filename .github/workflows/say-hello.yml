name: hello-world
on: 
    push:
        branches:[main]
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v2
        - name: say hello
          run: make say-hello

